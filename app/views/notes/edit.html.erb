<h1>Edit your Note</h1>

<%= form_for @note do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %> </br>
	<%= f.label :body %> 
	<%= f.text_area :body %></br>
	<%= f.submit "Update"%>
<% end %>
