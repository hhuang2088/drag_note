<h1>Create Note</h1>

<%= form_for @note do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %> </br>
	<%= f.label :body %> 
	<%= f.text_area :body %></br>
	<%= f.submit %>
<% end %>

<%= link_to "Home", root_path %>
